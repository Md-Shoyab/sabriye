import 'package:get/state_manager.dart';

class MemberShipAccordionItem {
  String? header;
  String? body;
  RxBool? isExpanded;

  MemberShipAccordionItem(this.header, this.body, this.isExpanded);
}
